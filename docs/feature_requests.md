# Plottle — Feature Requests

> **Inbox for feature ideas and quality-of-life requests.**
>
> Add new requests under `## New` at any time — mid-session, between sessions,
> whenever an idea surfaces. Be as specific as you can: describe the desired
> behavior, not just the end goal.
>
> The AI agent processes this file at the end of every session as part of the
> Closing Protocol: items in `## New` get routed to the right place in the task
> system (typically the Tier B Backlog in `TODO/TODO.md`) and moved verbatim to
> `## Incorporated into TODO`.

---

## New

<!-- Add new feature requests here. One bullet per request.
     Helpful to be specific about the desired behavior:
     Example (vague):    * Better search
     Example (specific): * Add a filter to the left panel to show only items
                           that haven't been used in any exam yet -->

*(no new requests)*

---

## Incorporated into TODO

<!-- Items moved here verbatim from New after being routed to a roadmap,
     backlog, or task list. Do not edit original text. Never delete entries —
     this section is a permanent record. -->

> **Historical items** — imported from `issue_tracking.md` (all implemented before this inbox was created):

* Flip color palette direction
* More specific fonts to select from (not just serif, etc.) - Arial or something close at least
* Custom palette creator tool
* Ability to export up to 600 dpi
* Change thickness of contour lines
* Make example dataset import dialogue collapsible
* Break Data Preview and Quick Statistics into its own side menu bar
* Make settings persist when switching between tabs
* Ability to save current plot settings as a template to apply later
* Gallery tab with example figures
* More elegant font in the app itself (something closer to Avenir if possible)
* Ability to zoom in/out on static plots (matplotlib) without needing to rely on browser zoom functionality
* Data upload tab takes a while to load - need improved caching system
* Have quick plot auto-assume plot of interest (instead of always showing histogram in the dropdown right away for matplotlib, have the natural option show based on the data type - if many datapoints, probably a line or scatter plot instead of histogram)
* Style presets is broken - the presets can be saved, but loading and applying a previously saved one does not work. Loading a preset should result in the plot type being set too
