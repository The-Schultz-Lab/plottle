# Plottle - Requirements
# Scientific data visualization and analysis toolkit

# Core Scientific Computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Visualization Libraries
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# GUI Framework
streamlit>=1.25.0

# Development and Testing
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.4.0
mypy>=1.5.0
pre-commit>=3.5.0
types-PyYAML>=6.0.0
types-requests>=2.28.0

# Excel support for Pandas
openpyxl>=3.1.0

# HTTP requests (used by NIST WebBook integration)
requests>=2.28.0

# Documentation — install with: pip install plottle[docs]
# sphinx>=7.0.0
# sphinx-rtd-theme>=1.3.0

# Instrument File Import — M17 (optional — install what you need)
# jcamp>=1.2.0      # JCAMP-DX (.jdx, .dx): IR, Raman, NMR, MS spectra  [parser unreliable in CI]
h5py>=3.0.0       # HDF5 (.h5, .hdf5): simulation outputs, large arrays
xarray>=0.20.0    # NetCDF (.nc, .cdf): atmospheric/oceanographic data
netcdf4>=1.6.0    # NetCDF engine used by xarray
# spc>=0.2          # Thermo Fisher SPC (.spc): Raman/IR spectra  [unmaintained; requires ez_setup]

# Mass Spectrometry — mzML/mzXML (optional)
# pymzml>=2.5.0    # mzML/mzXML mass spectrometry files

# Molecular Visualization — M18 (optional)
# py3Dmol>=2.0.0   # 3D molecular viewer (use with stmol in Streamlit)
# stmol>=0.0.9     # Streamlit wrapper for py3Dmol

# Statistical Modeling (optional: two-way ANOVA)
# statsmodels>=0.14.0  # optional: two-way ANOVA (anova_twoway in math.py)
